public class GreenCircle implements DrawAPI {
	
   @Override
   public void drawCircle(String name,int radius, int x, int y) {
      System.out.println(name + ": Drawing Circle[ color: green, radius: " + radius + ", x: " + x + ", " + y + "]");
   }
}
